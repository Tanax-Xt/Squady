export function TypographyInlineCode(props: React.ComponentProps<"code">) {
  return (
    <code
      className="relative rounded-sm bg-muted px-[0.3rem] py-[0.2rem] font-mono text-sm font-semibold"
      {...props}
    />
  );
}
