import { UserProfileSkeleton } from "@/widgets/profile";

const ResumeProfilePageLoading: React.FunctionComponent = () => {
  return <UserProfileSkeleton />;
};

export default ResumeProfilePageLoading;
